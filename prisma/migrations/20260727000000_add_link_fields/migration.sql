-- Add link domain fields to CardButton
ALTER TABLE "CardButton"
  ADD COLUMN IF NOT EXISTS "type" TEXT NOT NULL DEFAULT 'CUSTOM',
  ADD COLUMN IF NOT EXISTS "displayMode" TEXT NOT NULL DEFAULT 'BUTTON',
  ADD COLUMN IF NOT EXISTS "color" TEXT,
  ADD COLUMN IF NOT EXISTS "openInNewTab" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "analyticsEnabled" BOOLEAN NOT NULL DEFAULT false;
