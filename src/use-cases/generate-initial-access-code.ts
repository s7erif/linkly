import type { IssuedAccessCodeDTO } from "@/dto";
import { InitialAccessCodeExistsError, NotFoundError } from "@/lib/errors";
import type { TransactionRepositories, UnitOfWork } from "@/repositories";
import type { AccessCodeGenerator, SecretHasher } from "@/services/credential-security.service";
import { generateInitialAccessCodeSchema, type GenerateInitialAccessCodeInput } from "@/validation";
import { parseUseCaseInput } from "./shared";
export class GenerateInitialAccessCode { constructor(private readonly unitOfWork:UnitOfWork,private readonly hasher:SecretHasher,private readonly generator:AccessCodeGenerator){} execute(input:GenerateInitialAccessCodeInput):Promise<IssuedAccessCodeDTO>{return this.unitOfWork.execute(repositories=>this.executeIn(repositories,input))}async executeIn(repositories:Pick<TransactionRepositories,"accessCodes"|"cards">,input:GenerateInitialAccessCodeInput):Promise<IssuedAccessCodeDTO>{const command=parseUseCaseInput(generateInitialAccessCodeSchema,input);const plaintext=this.generator.generate();const codeHash=await this.hasher.hash(plaintext);if(!await repositories.cards.findById(command.cardId,null))throw new NotFoundError("Card",command.cardId);if(await repositories.accessCodes.findMaximumVersion(command.cardId)!==null)throw new InitialAccessCodeExistsError(command.cardId);const accessCode=await repositories.accessCodes.create({cardId:command.cardId,codeHash,version:1,expiresAt:command.expiresAt});return {accessCode,code:this.generator.format(plaintext)}}}
