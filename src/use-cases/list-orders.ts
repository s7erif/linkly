import type {OrderDTO} from "@/dto";import type {OrderReadRepository} from "@/repositories";import {listOrdersSchema,type ListOrdersInput} from "@/validation/order";import {parseUseCaseInput} from "./shared";export class ListOrders{constructor(private readonly orders:OrderReadRepository){}execute(input:ListOrdersInput={}):Promise<readonly OrderDTO[]>{const criteria=parseUseCaseInput(listOrdersSchema,input);return this.orders.list(criteria)}}
