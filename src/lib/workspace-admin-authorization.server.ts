import"server-only";import type{AuthOptions}from"next-auth";import{getServerSession}from"next-auth/next";import{authOptions}from"@/lib/auth";import type{EditorAuthorizationContext}from"@/use-cases/editor-authorization";export async function getWorkspaceAdminAuthorization():Promise<EditorAuthorizationContext|undefined>{const session=await getServerSession(authOptions as AuthOptions);return session?.user?.email?{adminEmail:session.user.email}:undefined}
