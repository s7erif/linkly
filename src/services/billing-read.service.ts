import type { InvoiceRepository } from "@/repositories/invoice.repository"; import type { PaymentRepository } from "@/repositories/payment.repository"; export class BillingReadService{constructor(private readonly invoices:InvoiceRepository,private readonly payments:PaymentRepository){}async customerBilling(customerId:string){return{invoices:await this.invoices.findByCustomer(customerId),payments:[] as const}}async invoice(id:string){return this.invoices.find(id)}async payment(id:string){return this.payments.find(id)}}
