import type { NotificationService } from "@/notifications/notification.service"; export class BillingNotificationService{constructor(private readonly notifications:NotificationService){}async paymentApproved(input:Parameters<NotificationService["sendWelcome"]>[0]){return this.notifications.sendWelcome(input)}async invoiceReady(input:Parameters<NotificationService["sendWelcome"]>[0]){return this.notifications.sendWelcome(input)}async accessCodeReady(input:Parameters<NotificationService["sendWelcome"]>[0]){return this.notifications.sendWelcome(input)}}
