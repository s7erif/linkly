import { AdminCard, AdminPageHeader } from "@/design/components"; import { mediaService } from "@/lib/composition-root"; import s from "@/features/admin/admin-records.module.css"; export default async function Folders(){const folders=await mediaService.listFolders();return <><AdminPageHeader eyebrow="Media" title="Folders" description="Organize assets with nested folders."/><AdminCard title="Folder tree"><p className={s.muted}>Create, rename, move, and delete folders through the Media application service.</p>{folders.length?<div className={s.sectionStack}>{folders.map(folder=><div className={s.summaryCard} key={folder.id}><strong>{folder.name}</strong><small>{folder.parentId?"Nested folder":"Root folder"}</small></div>)}</div>:<div className={s.empty}><strong>No folders yet</strong><p>Folders are created automatically when the first payment proof is uploaded.</p></div>}</AdminCard></>}
