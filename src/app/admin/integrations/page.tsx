import { AdminCard, AdminPageHeader } from "@/design/components"; import s from "@/features/admin/admin-records.module.css";
export default function IntegrationsPage(){return <><AdminPageHeader eyebrow="Platform" title="Integrations" description="Configuration status for external platform providers."/><section className={s.grid}>{["Resend","Supabase","Storage","Stripe","Paymob"].map(name=><AdminCard key={name} title={name}><p className={s.muted}>{name === "Resend" ? "Configured through the Notification Service." : "Configuration surface ready; integration is not enabled."}</p><span className={s.adminBadge}>{name === "Resend" ? "Configured" : "Not configured"}</span></AdminCard>)}</section></>}
