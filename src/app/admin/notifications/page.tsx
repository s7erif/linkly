import { adminReadService } from "@/lib/composition-root"; import { AdminCard, AdminPageHeader, AdminStatCard } from "@/design/components"; import s from "@/features/admin/admin-records.module.css";
export default async function NotificationsPage(){const data=await adminReadService.dashboard();return <><AdminPageHeader eyebrow="Platform" title="Notifications" description="Monitor delivery health for customer communications."/><section className={s.metricGrid}><AdminStatCard label="Pending" value="—" hint="No aggregate read model"/><AdminStatCard label="Sent" value="—" hint="Delivery history" tone="positive"/><AdminStatCard label="Failed" value="—" hint="Retry queue" tone="accent"/></section><AdminCard title="Notification center"><p className={s.muted}>Notification delivery records remain available through order details. A dedicated paginated delivery read model is the next operational increment.</p><p className={s.muted}>Orders currently tracked: {data.recentOrders.length} recent records.</p></AdminCard></>}
